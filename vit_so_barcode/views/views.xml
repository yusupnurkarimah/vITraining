<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vit_so_barcode.list">
      <field name="name">vit_so_barcode list</field>
      <field name="model">vit_so_barcode.vit_so_barcode</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vit_so_barcode.action_window">
      <field name="name">vit_so_barcode window</field>
      <field name="res_model">vit_so_barcode.vit_so_barcode</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vit_so_barcode.action_server">
      <field name="name">vit_so_barcode server</field>
      <field name="model_id" ref="model_vit_so_barcode_vit_so_barcode"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vit_so_barcode" id="vit_so_barcode.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vit_so_barcode.menu_1" parent="vit_so_barcode.menu_root"/>
    <menuitem name="Menu 2" id="vit_so_barcode.menu_2" parent="vit_so_barcode.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vit_so_barcode.menu_1_list" parent="vit_so_barcode.menu_1"
              action="vit_so_barcode.action_window"/>
    <menuitem name="Server to list" id="vit_so_barcode" parent="vit_so_barcode.menu_2"
              action="vit_so_barcode.action_server"/>
    -->
  </data>
</odoo>